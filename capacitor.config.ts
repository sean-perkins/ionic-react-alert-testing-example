import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-testing-alert',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
